import * from "atom:std";


func fact(n) {
    if (n <= 1) {
        return 1;
    }
    return n * fact(n - 1);
}

func fib(n) {
    if (n <= 1) {
        return n;
    }
    return fib(n - 1) + fib(n - 2);
}

std.println(fact(5));
std.println(fib(10));
