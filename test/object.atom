import [ println ] from "atom:std";
import [ freeze ] from "atom:object";

const obj = {
    name: "John",
    age: 30,
    city: "New York"
};

const frz = freeze(obj);

obj.name = "Jane";

println(obj);
println(frz, frz == obj);

const selfRef = {
    prop: "PROP",
    self: ""
};

selfRef["self"] = selfRef;

println(selfRef);