import "atom:std";

var global = 2;

func factorial(n) {
    if (n <= 1) {
        return 1;
    }
    std;
    global = n;
    std.println(global);
    return n * (factorial(n - 1) catch (err) {
        std.throw(err);
    });
}

func upper() {
    lower();
}

func lower() {
    std.println("imports -> Greetings from lower!!");
}


upper();