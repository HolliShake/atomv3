import [ println ] from "atom:std";


var global = 2;


func mutate() {
    {
        println(global);
        global += 100;
        println(global);
    }
}

println(global);
std.println(global);

mutate();
println(global);